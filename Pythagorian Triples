from math import sqrt

num = int(input("Enter the number upto which you want to find the Pythagorean triples: "))

for a in range(10,num+1):
    for b in range(a, num+1):
        z = a**2 + b**2
        c = int(sqrt(z))
        if z == c**2:
          print(a ,b ,c)
